<Window x:Class="PL.Order.OrderForListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL.Order"
        mc:Ignorable="d"
        DataContext="{Binding orders, RelativeSource={RelativeSource Mode=Self}}"
        Title="OrderForListWindow" Height="450" Width="800">
    <StackPanel>
        <ListView x:Name="ListviewOrders" ItemsSource="{Binding}" Margin="0,41,0,0" Grid.RowSpan="2" SelectionChanged="ListviewOrders_SelectionChanged" ></ListView>
    </StackPanel>
</Window>
