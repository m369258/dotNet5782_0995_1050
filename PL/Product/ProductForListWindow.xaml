<Window x:Class="PL.Product.ProductForListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL.Product"
        mc:Ignorable="d"
        DataContext="{Binding Products, RelativeSource={RelativeSource Mode=Self}}"
        Title="ProductForListWindow" Height="640" Width="800">
    <Window.Resources>
        <DataTemplate x:Key="MyDataTemplate" DataType="local:MyData">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="2*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Name:" />
                <TextBlock Grid.Column="1" Text="{Binding Path=Name}" />
                <TextBlock Grid.Column="2" Text="Price:" />
                <TextBlock Grid.Column="3" Text="{Binding Path=Price}" />
                <TextBlock Grid.Column="4" Text="Category:"/>
                <TextBlock Grid.Column="5" Text="{Binding Path=category}" />
            </Grid>
        </DataTemplate>
    </Window.Resources>
    <StackPanel>
        <Label x:Name="lblCategory" Content="קטגוריה" Grid.Column="1" HorizontalAlignment="Center" FontSize="16" FontWeight="Bold" Margin="0,10,0,0" RenderTransformOrigin="2.701,0.04"/>
        <ComboBox x:Name="cmxCategorySelector" SelectionChanged="CategorySelector_SelectionChanged" Height="46" FontSize="16" Width="206" RenderTransformOrigin="-0.305,0.775"/>
        <ListBox x:Name="ProductListview" ItemsSource="{Binding}" ItemTemplate="{StaticResource MyDataTemplate}"
HorizontalContentAlignment="Stretch" SelectionChanged="ProductListview_SelectionChanged_1" />
        <Button x:Name="btnAddProduct" Content="הוספת פריט חדש" Grid.Row="2" Margin="314,5,0,5" Padding="5" Click="Button_Click" Background="Black" Foreground="White" FontFamily="Algerian" FontSize="16" FontWeight="Bold" HorizontalAlignment="Left" BorderBrush="#FFF652E2" Width="184"/>
    </StackPanel>
</Window>
   